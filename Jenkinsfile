stage 'snapshot build'
node {
    echo 'Hello from snapshot'
}

stage 'snapshot deploy'
node {
    echo 'Hello from snapshot deploy'
}


input message: "PR?"

stage 'PR to master'
node {
    echo 'Hello from snapshot'
}
